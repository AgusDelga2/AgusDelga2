# ============================================================
# 🧠 VERSIÓN A — Neural Pulse
# Estética: red neuronal, colores azul/violeta
# ============================================================

name: 🧠 Neural Pulse — Contribution Graph

on:
  schedule:
    - cron: "0 */12 * * *"   # Se actualiza cada 12 horas
  workflow_dispatch:           # También podés ejecutarlo manualmente
  push:
    branches:
      - main

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: 🧠 Generate Neural Pulse — Dark
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: AgusDelga2
          outputs: |
            dist/neural-pulse-dark.svg?color_snake=#a78bfa&color_dots=#0d1117,#1e1b4b,#3730a3,#6d28d9,#a78bfa

      - name: 🧠 Generate Neural Pulse — Light
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: AgusDelga2
          outputs: |
            dist/neural-pulse-light.svg?color_snake=#7c3aed&color_dots=#f5f3ff,#ddd6fe,#8b5cf6,#6d28d9,#4c1d95

      - name: 🚀 Push to output branch
        uses: crazy-max/ghaction-github-pages@v3
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
